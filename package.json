{
  "name": "gh-stats",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('Run: npm run vercel:dev')\"",
    "vercel:dev": "vercel dev",
    "lint": "node -e \"console.log('No lint configured')\"",
    "test": "node -e \"console.log('No tests configured')\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": "24.x"
  }
}
